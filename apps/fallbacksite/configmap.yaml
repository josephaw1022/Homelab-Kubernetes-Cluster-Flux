apiVersion: v1
kind: ConfigMap
metadata:
  name: fallback-html
  namespace: fallbacksite
data:
  index.html: |
    <!doctype html>
    <html lang="en">
      <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Homelab Fallback</title>
        <script src="https://cdn.tailwindcss.com"></script>
      </head>
      <body class="bg-gray-900 text-gray-100 flex items-center justify-center min-h-screen">
        <div class="text-center space-y-4">
          <h1 class="text-5xl font-extrabold text-blue-400">🏠 Homelab Fallback Site</h1>
          <p class="text-lg text-gray-300">
            This is the default catch-all page for <span class="font-semibold text-blue-300">my homelab</span>.
          </p>
          <p class="text-gray-400">
            If you’re seeing this, the hostname you tried isn’t mapped to an active Ingress.
          </p>
          <div class="mt-6">
            <a href="https://${ROOT_DOMAIN}" class="px-5 py-2 rounded-md bg-blue-600 hover:bg-blue-700 text-white font-medium transition">
              Return to Home
            </a>
          </div>
        </div>
      </body>
    </html>
